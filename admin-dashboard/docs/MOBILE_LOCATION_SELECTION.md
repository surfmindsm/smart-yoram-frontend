# 커뮤니티 지역 선택 기능 명세서 (모바일 개발자용)

> **작성일**: 2025-10-08
> **대상**: 모바일 앱 개발자 (Flutter/React Native)
> **목적**: 커뮤니티 기능(무료나눔, 물품판매, 물품요청 등)의 지역 선택 UI 및 데이터 구조 설명

---

## 📋 개요

커뮤니티 기능에서 사용되는 **2단계 지역 선택 시스템**입니다. 사용자가 도/시를 먼저 선택하고, 그에 따른 시/군/구를 선택하는 방식입니다.

### 적용 화면
- 무료나눔(드림) 등록/수정
- 물품판매 등록/수정
- 물품요청 등록/수정
- 구인공고 등록/수정
- 구직신청 등록/수정
- 행사팀 모집/지원 등록/수정
- 행사 소식 등록/수정

---

## 🗂 데이터베이스 필드

커뮤니티 테이블에는 다음 3개의 지역 관련 필드가 있습니다:

```sql
province          character varying(50)   NULL,  -- 도/시 (예: "서울특별시", "경기도")
district          character varying(100)  NULL,  -- 시/군/구 (예: "강남구", "성남시")
delivery_available boolean                NULL DEFAULT false  -- 택배 가능 여부
```

### 필드 설명

| 필드 | 타입 | 필수 | 설명 | 예시 |
|-----|------|------|------|------|
| `province` | `string(50)` | 선택 | 도/시 선택값 | `"서울특별시"`, `"경기도"`, `"제주특별자치도"` |
| `district` | `string(100)` | 선택 | 시/군/구 선택값 | `"강남구"`, `"성남시"`, `"제주시"` |
| `delivery_available` | `boolean` | 선택 | 택배 거래 가능 여부 | `true`, `false` |

---

## 🎨 UI 구현 가이드

### 1. 지역 선택 폼 (등록/수정 화면)

#### 레이아웃
```
┌─────────────────────────────────────────┐
│  [도/시 선택 ▼]    [시/군/구 선택 ▼]    │
│                                         │
│  ☐ 택배 가능                            │
└─────────────────────────────────────────┘
```

#### 컴포넌트 구성
1. **도/시 선택 드롭다운**
   - 전체 17개 도/시 목록 표시
   - 초기값: 빈 상태 또는 "도/시 선택"
   - 선택 시 → 시/군/구 드롭다운 활성화

2. **시/군/구 선택 드롭다운**
   - 도/시 미선택 시: 비활성화
   - 도/시 선택 시: 해당 도/시의 시/군/구 목록 표시
   - 도/시 변경 시: 현재 선택값 초기화

3. **택배 가능 체크박스**
   - 체크하면 `delivery_available = true`
   - 지역을 선택하지 않아도 체크 가능

#### 동작 플로우
```
1. 사용자가 "경기도" 선택
   → province = "경기도"
   → 시/군/구 드롭다운 활성화 (31개 시/군 표시)

2. 사용자가 "성남시" 선택
   → district = "성남시"

3. 사용자가 "택배 가능" 체크
   → delivery_available = true

최종 저장 데이터:
{
  "province": "경기도",
  "district": "성남시",
  "delivery_available": true
}
```

---

### 2. 지역 필터 (목록 화면)

#### 레이아웃
```
┌──────────────────────────────────────────────────┐
│  [카테고리 ▼]  [전체 도/시 ▼]  [전체 구 ▼]      │
└──────────────────────────────────────────────────┘
```

#### 필터 옵션
- **도/시 필터**: "전체 도/시" + 17개 도/시
- **시/군/구 필터**: "전체 구" + 선택된 도/시의 시/군/구 목록
  - 도/시 미선택 시: 비활성화 또는 "전체 구"만 표시

#### 필터링 로직 (예시)
```javascript
// 도/시 필터
if (selectedProvince && selectedProvince !== '전체 도/시') {
  items = items.filter(item => item.province === selectedProvince);
}

// 시/군/구 필터
if (selectedDistrict && selectedDistrict !== '전체 구') {
  items = items.filter(item => item.district === selectedDistrict);
}

// 택배 가능 항목 포함
// delivery_available = true인 항목은 모든 지역에 노출 가능
items = items.filter(item =>
  (item.province === selectedProvince && item.district === selectedDistrict) ||
  item.delivery_available === true
);
```

---

### 3. 지역 정보 표시 (상세 화면, 목록 카드)

#### 표시 우선순위
```
1. province + district가 있으면 → "서울특별시 강남구"
2. delivery_available만 있으면 → "전국 택배가능"
3. province만 있으면 → "서울특별시"
4. 아무것도 없으면 → "-" 또는 "지역 미지정"
```

#### 예시 코드 (Flutter)
```dart
String getLocationDisplay({
  String? province,
  String? district,
  bool? deliveryAvailable,
}) {
  // 1. 도/시 + 구 조합
  if (province != null && province.isNotEmpty) {
    if (district != null && district.isNotEmpty) {
      // 택배 가능 추가 표시
      if (deliveryAvailable == true) {
        return '$province $district (전국 택배가능)';
      }
      return '$province $district';
    }
    // 도/시만 있는 경우
    if (deliveryAvailable == true) {
      return '$province (전국 택배가능)';
    }
    return province;
  }

  // 2. 택배만 체크된 경우
  if (deliveryAvailable == true) {
    return '전국 택배가능';
  }

  // 3. 기본값
  return '-';
}
```

---

## 📡 API 요청/응답 형식

### 게시글 등록 (POST)
```json
{
  "title": "노트북 판매합니다",
  "description": "사용감 적은 노트북입니다",
  "category": "전자제품",
  "price": 500000,
  "province": "서울특별시",
  "district": "강남구",
  "delivery_available": true,
  "contact_phone": "010-1234-5678"
}
```

### 게시글 조회 (GET)
```json
{
  "id": 123,
  "title": "노트북 판매합니다",
  "province": "서울특별시",
  "district": "강남구",
  "delivery_available": true,
  "created_at": "2025-10-08T10:30:00Z"
}
```

### 필터링 조회 (GET with Query Parameters)
```
GET /api/community/sharing?province=경기도&district=성남시
GET /api/community/sharing?delivery_available=true
```

---

## 🗺 전체 지역 데이터

### 도/시 목록 (17개)
```json
[
  "서울특별시",
  "부산광역시",
  "대구광역시",
  "인천광역시",
  "광주광역시",
  "대전광역시",
  "울산광역시",
  "세종특별자치시",
  "경기도",
  "강원도",
  "충청북도",
  "충청남도",
  "전라북도",
  "전라남도",
  "경상북도",
  "경상남도",
  "제주특별자치도"
]
```

### 시/군/구 예시

#### 서울특별시 (25개 구)
```json
[
  "강남구", "강동구", "강북구", "강서구", "관악구",
  "광진구", "구로구", "금천구", "노원구", "도봉구",
  "동대문구", "동작구", "마포구", "서대문구", "서초구",
  "성동구", "성북구", "송파구", "양천구", "영등포구",
  "용산구", "은평구", "종로구", "중구", "중랑구"
]
```

#### 경기도 (31개 시/군)
```json
[
  "수원시", "성남시", "용인시", "고양시", "안양시",
  "안산시", "부천시", "광명시", "평택시", "과천시",
  "오산시", "시흥시", "군포시", "의왕시", "하남시",
  "남양주시", "구리시", "의정부시", "양주시", "동두천시",
  "파주시", "포천시", "연천군", "김포시", "화성시",
  "광주시", "이천시", "여주시", "양평군", "가평군", "안성시"
]
```

#### 제주특별자치도 (2개 시)
```json
["제주시", "서귀포시"]
```

**전체 시/군/구 데이터는 별도 첨부 파일 참조**
→ `koreaLocations.json` (전체 도/시별 시/군/구 데이터)

---

## 💡 구현 팁

### 1. 드롭다운 최대 높이 설정
- 항목이 많으므로 최대 높이 제한 필요 (권장: 240-300px)
- 스크롤 가능하도록 구현

### 2. 도/시 변경 시 시/군/구 초기화
```dart
void onProvinceChanged(String? newProvince) {
  setState(() {
    selectedProvince = newProvince;
    selectedDistrict = null; // 반드시 초기화
  });
}
```

### 3. 택배 가능 항목 필터링
- 택배 가능 항목은 모든 지역 검색에 포함되어야 함
- 지역 필터와 OR 조건으로 처리

### 4. 지역 데이터 로딩 방법
옵션 1: 앱 내장 (권장)
- 지역 데이터는 정적이므로 앱에 JSON 파일로 포함
- 빠른 로딩, 오프라인 지원

옵션 2: API 제공
```
GET /api/locations/provinces
GET /api/locations/districts?province=서울특별시
```

---

## 📱 Flutter 구현 예시

```dart
class LocationSelector extends StatefulWidget {
  final Function(String? province, String? district) onLocationChanged;

  @override
  _LocationSelectorState createState() => _LocationSelectorState();
}

class _LocationSelectorState extends State<LocationSelector> {
  String? selectedProvince;
  String? selectedDistrict;
  bool deliveryAvailable = false;

  final List<String> provinces = [
    '서울특별시', '부산광역시', '대구광역시', '인천광역시',
    '광주광역시', '대전광역시', '울산광역시', '세종특별자치시',
    '경기도', '강원도', '충청북도', '충청남도',
    '전라북도', '전라남도', '경상북도', '경상남도', '제주특별자치도'
  ];

  Map<String, List<String>> districtData = {
    '서울특별시': ['강남구', '강동구', '강북구', /* ... */],
    '경기도': ['수원시', '성남시', '용인시', /* ... */],
    // ... 전체 데이터
  };

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        // 도/시 + 시/군/구 드롭다운
        Row(
          children: [
            Expanded(
              child: DropdownButton<String>(
                hint: Text('도/시 선택'),
                value: selectedProvince,
                isExpanded: true,
                items: provinces.map((province) {
                  return DropdownMenuItem(
                    value: province,
                    child: Text(province),
                  );
                }).toList(),
                onChanged: (value) {
                  setState(() {
                    selectedProvince = value;
                    selectedDistrict = null; // 초기화
                    widget.onLocationChanged(selectedProvince, selectedDistrict);
                  });
                },
              ),
            ),
            SizedBox(width: 8),
            Expanded(
              child: DropdownButton<String>(
                hint: Text('시/군/구 선택'),
                value: selectedDistrict,
                isExpanded: true,
                items: (selectedProvince != null && districtData.containsKey(selectedProvince))
                    ? districtData[selectedProvince]!.map((district) {
                        return DropdownMenuItem(
                          value: district,
                          child: Text(district),
                        );
                      }).toList()
                    : [],
                onChanged: selectedProvince == null ? null : (value) {
                  setState(() {
                    selectedDistrict = value;
                    widget.onLocationChanged(selectedProvince, selectedDistrict);
                  });
                },
              ),
            ),
          ],
        ),

        // 택배 가능 체크박스
        CheckboxListTile(
          title: Text('택배 가능'),
          value: deliveryAvailable,
          onChanged: (value) {
            setState(() {
              deliveryAvailable = value ?? false;
            });
          },
        ),
      ],
    );
  }
}
```

---

## 🚨 주의사항

1. **필드는 모두 선택 사항**: `province`, `district`, `delivery_available` 모두 NULL 가능
2. **도/시 변경 시 시/군/구 초기화 필수**: UX 혼란 방지
3. **택배 가능 항목은 전국 노출**: 필터링 시 OR 조건으로 처리
4. **특별시/광역시는 구 단위, 도는 시/군 단위**: 데이터 구조 주의
5. **빈 값 처리**: province/district가 NULL이거나 빈 문자열일 수 있음

---

## 📎 참고 자료

- **전체 지역 데이터**: `koreaLocations.json` (별도 첨부)
- **웹 구현 참고**: `admin-dashboard/src/data/koreaLocations.ts`
- **기존 명세서**: `docs/location-selection-specification.md`

---

## 🔄 업데이트 이력

| 날짜 | 버전 | 변경 내용 |
|-----|------|----------|
| 2025-10-08 | 1.0 | 모바일 개발자용 문서 최초 작성 |

---

**문의**: 백엔드 개발팀 또는 프로젝트 매니저
